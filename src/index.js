import React from 'react';
import ReactDOM from 'react-dom/client';
import ConsultationApp from './ConsultationApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<ConsultationApp />);